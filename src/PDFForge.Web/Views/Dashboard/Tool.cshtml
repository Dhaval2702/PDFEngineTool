@{
    ViewData["Title"] = $"Tool - {ViewData["ToolName"]}";
}

<h2 class="mb-3 text-capitalize">@ViewData["ToolName"]</h2>
<p class="text-muted">This route is ready for tool-specific workflows in future phases.</p>

<partial name="~/Views/Components/FileUploader/Default.cshtml" />
